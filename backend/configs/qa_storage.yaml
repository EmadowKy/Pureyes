# QA Storage Configuration
storage:
  # 每个用户最多保存的问答记录数
  max_records_per_user: 100
  
  # 存储数据的根目录（相对于 backend 目录）
  storage_dir: "output/qa_records"
  
  # 是否启用自动备份
  enable_backup: true
  
  # 备份目录
  backup_dir: "output/qa_backups"
